[project]
name = "m5-monthly-sku-forecast"
version = "0.1.0"
description = "Monthly SKU demand forecasting using tabular time-series features (M5 / Walmart)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.6",
    "pandas>=3.0.3",
]
